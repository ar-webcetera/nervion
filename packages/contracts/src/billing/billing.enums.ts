export enum TaskBillingType {
  HOURLY = "hourly",
  FIXED = "fixed",
}

export enum BillingReviewStatus {
  PENDING = "pending",
  APPROVED = "approved",
  REJECTED = "rejected",
}

export enum RevenueSourceType {
  TIMELOG = "timelog",
  FIXED_TASK = "fixed_task",
}
